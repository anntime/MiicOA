<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.miic.share.activity.FriendsApplyActivity">
    <include layout="@layout/page_title_bar" />
    <ListView
        android:id="@+id/apply_list"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginTop="10dp"
        android:divider="@null"
        android:visibility="gone"></ListView>
    <TextView
        android:id="@+id/message_tip"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center_vertical"
        android:gravity="center"
        android:text="暂没有通讯录验证消息哦~"
        android:textColor="@color/news_detail_content"
        android:textSize="16sp"
        android:visibility="gone"/>
</LinearLayout>
